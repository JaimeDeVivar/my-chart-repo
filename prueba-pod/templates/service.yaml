apiVersion: v1
kind: Service
metadata:
  name: {{ include "prueba-pod.fullname" . }}
  labels:
    {{- include "prueba-pod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prueba-pod.selectorLabels" . | nindent 4 }}
