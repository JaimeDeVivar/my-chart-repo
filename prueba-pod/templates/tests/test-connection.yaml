apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prueba-pod.fullname" . }}-test-connection"
  labels:
    {{- include "prueba-pod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prueba-pod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
